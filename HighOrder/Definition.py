"""| Function   | Purpose                   | Returns         |
| ---------- | ------------------------- | --------------- |
| `map()`    | Transform every element   | Iterable        |
| `filter()` | Keep matching elements    | Iterable        |
| `reduce()` | Combine elements into one | Single value    |
| `sorted()` | Arrange elements in order | New sorted list |"""
